{
  "name": "internship-tracker-backend",
  "version": "0.1.0",
  "main": "server.js",
  "scripts": {
    "prestart": "npm run build:frontend",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:backup-migrate": "node scripts/migrate_and_backup.js",
    "build:frontend": "npm --prefix internship-frontend install && npm --prefix internship-frontend run build",
    "render-build": "npm install && npm run build:frontend"
  },
  "dependencies": {
    "@faker-js/faker": "^10.3.0",
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.9.13",
    "socket.io": "^4.7.2",
    "sqlite3": "^5.1.6",
    "twilio": "^5.12.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
