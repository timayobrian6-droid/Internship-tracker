# Development environment variables
DB_FILE=internship_final.db
PORT=5000
JWT_SECRET=change_this_to_a_strong_secret
ADMIN_EMAIL=admin@local
ADMIN_PASS=adminpass

# Base URL used for password reset links
RESET_BASE_URL=http://localhost:3001
# Use "console" to log reset links instead of sending email
RESET_EMAIL_MODE=console

# When migrating to Postgres, add DATABASE_URL or PGHOST/PGUSER etc.
